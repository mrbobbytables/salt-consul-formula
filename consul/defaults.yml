agent:
  checks: []
  config:
    data_dir: /var/lib/consul
    ui: true
  log:
  log_dir:
  data_dir: /var/lib/consul
  pkg:
    install: true
    service: true
    version: 0.6.3
  opts:
    config-dir:
      - /etc/consul/agent.d
  scripts: []
  scripts_dir: /usr/share/consul/agent/scripts
  services: []
  ssl:
    ca: 
      name:
      source:
    cert:
      name:
      source:
    dir: /etc/consul/ssl
    enabled: true
    key:
      name:
      source:



template:
  config: {}
  log:
  log_dir:
  pkg:
    install: false
    service: true
    version: 0.14.0
  opts:
    config:
      - /etc/consul/template.d
  ssl:
    ca: 
      name:
      source:
    cert:
      name:
      source:
    dir: /etc/consul/ssl
    enabled: true
    key:
      name:
      source:
  templates: []
  templates_dir: /usr/share/consul/template/templates

